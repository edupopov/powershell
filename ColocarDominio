# Se você precisar adicionar uma conta de usuário de domínio ao grupo de administradores locais, execute o seguinte comando em um prompt de comando (não na janela do PowerShell)
net localgroup administrators /add <DomainName>\<UserName>

# Ingressar um computador em um domínio
Netdom join% ComputerName% /Domain: <domain> /userd: <domain\username> /passwordd:*

# Remover um computador de um domínio
Netdom remover <computername>

# Configurar o arquivo de paginação
WMIC filepageset em que Name = " <path/filename> " Set initialSize = <initialsize> , MaximumSize =<maxsize>

# Ativar o servidor remotamente
cscript slmgr. vbs – ipk <product key><server name><username><password>
cscript slmgr. vbs-ato <servername> <username><password>

# https://docs.microsoft.com/pt-br/windows-server/administration/server-core/server-core-administer
